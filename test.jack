class Main {
    static int x;
    field int y, z;

    function int add(int a, int b, Main m) {
        var int x;
        var int y;

        let x = a + b;

        if (x > 0) {
            let y = x;
        }

        if (a = b) {
            let y = a;
        } else {
            let y = b;
        }
    }
}